# PiSovereign Docker Environment Configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# =============================================
# Required — Domain & TLS
# =============================================

# Your domain pointing to this server (used by Traefik for TLS)
PISOVEREIGN_DOMAIN=pi.example.com

# Email for Let's Encrypt certificate notifications
TRAEFIK_ACME_EMAIL=admin@example.com

# =============================================
# PiSovereign Image
# =============================================

# GitHub Container Registry owner (default: twohreichel)
GHCR_OWNER=twohreichel

# PiSovereign image version (default: latest)
PISOVEREIGN_VERSION=latest

# =============================================
# Vault — Secret Store
# =============================================

# Vault root token (generated on first start, see vault-keys.json)
# After first start, copy the root_token from:
#   docker/vault-init-output/vault-keys.json
VAULT_TOKEN=

# =============================================
# Monitoring (only with --profile monitoring)
# =============================================

# Grafana admin password
GRAFANA_ADMIN_PASSWORD=changeme
